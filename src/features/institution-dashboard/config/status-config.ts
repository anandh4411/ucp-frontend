// src/features/dashboard/config/status-config.ts
// Reuse the shared status configuration from phases
export {
  statusConfigs,
  getStatusConfig,
  orderedStatuses,
} from "../../phases/config/status-config";
